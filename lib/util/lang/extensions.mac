/* lambdas are just too verbose in Maxima */
/* usage: [a1,a2,a3] -> (a1 + a2 + a3) */
"->" (args, expr) ::= buildq([args, expr], lambda(args, expr)) $
infix("->") $

printlist(list) := for i in list do print(i) $